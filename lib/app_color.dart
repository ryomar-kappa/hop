import 'package:flutter/material.dart';

class AppColor {
  static const Color header = Color.fromRGBO(241, 196, 15, 1); // ゴールデンイエロー
  static const Color background = Color.fromRGBO(247, 238, 214, 1); // クリーム色
  static const Color textColor = Color.fromRGBO(62, 39, 35, 1); // ダークブラウン
  static const Color mainButton = Color.fromRGBO(230, 100, 25, 1); // オレンジブラウン
  static const Color disabledButton = Color.fromRGBO(167, 167, 167, 1); // 灰色
  static const Color generalActionButton = Color.fromRGBO(87, 143, 202, 1);
  static const Color buttonText = Color.fromRGBO(255, 255, 255, 1); // 白
  static const Color correctAnswer = Color.fromRGBO(76, 175, 80, 1); // 緑
  static const Color incorrectAnswer = Color.fromRGBO(231, 76, 60, 1); // 赤
  static const Color quizBackground = Color.fromRGBO(215, 182, 138, 1);
  static const Color quizFocusedBackground = Color.fromRGBO(200, 130, 40, 1);
  static const Color quizText = Color.fromRGBO(62, 39, 35, 1);
}
